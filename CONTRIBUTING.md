# Contributing

Run `npm install`, then `npm run check` before opening a pull request.

Never commit credentials, uploaded media, private certificates, or production database URLs. Changes to verdict semantics require tests covering both evidence interpretation and distribution policy.
